# API Reference

All APIs are under `/api` and tenant scoped with `x-tenant-id`.

Core endpoints: health, dashboard, assets, findings, remediation actions, simulation, plan generation, workflow routing, virtual patching, agentic planning, policies, reports, audit, connector dry-runs, and worker dry-runs.

The OpenAPI specification is available at `/docs` when the FastAPI service is running.
